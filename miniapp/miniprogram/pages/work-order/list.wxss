/* ========== 基础容器 ========== */
.work-order-list {
  min-height: 100vh;
  background: #f5f7fa;
  padding-bottom: 120rpx;
}

/* ========== 顶部标题栏 ========== */
.page-header {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 40rpx 32rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: 0 4rpx 12rpx rgba(102, 126, 234, 0.2);
}

.header-title {
  font-size: 40rpx;
  font-weight: bold;
  color: #fff;
  text-shadow: 0 2rpx 4rpx rgba(0,0,0,0.1);
}

.header-count {
  background: rgba(255, 255, 255, 0.3);
  color: #fff;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
  font-size: 24rpx;
  font-weight: bold;
}

/* ========== 搜索栏 ========== */
.search-bar {
  padding: 24rpx;
  background: #fff;
}

.search-container {
  display: flex;
  align-items: center;
  background: #f5f7fa;
  border-radius: 12rpx;
  padding: 20rpx 24rpx;
  border: 2rpx solid #e8e8e8;
  transition: all 0.3s;
}

.search-container:focus-within {
  border-color: #667eea;
  background: #fff;
}

.search-icon {
  font-size: 32rpx;
  margin-right: 12rpx;
  color: #999;
  flex-shrink: 0;
}

.search-input {
  flex: 1;
  font-size: 28rpx;
  color: #333;
}

/* ========== 状态筛选 ========== */
.status-filter {
  padding: 0 24rpx 24rpx;
  background: #fff;
}

.status-picker {
  width: 100%;
}

.picker-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #f5f7fa;
  padding: 20rpx 24rpx;
  border-radius: 12rpx;
  border: 2rpx solid #e8e8e8;
  min-height: 80rpx;
}

.picker-label {
  font-size: 28rpx;
  color: #666;
  font-weight: 500;
  flex-shrink: 0;
}

.picker-value {
  flex: 1;
  text-align: center;
  font-size: 28rpx;
  color: #333;
  font-weight: 500;
}

.picker-arrow {
  font-size: 20rpx;
  color: #999;
  flex-shrink: 0;
  margin-left: 12rpx;
}

/* ========== 工单列表容器 ========== */
.order-list {
  /* 🔑 关键修复：不使用scroll-view，改用普通view的纵向滚动 */
  /* 移除padding避免横向溢出，改为卡片的margin处理 */
  padding: 0;
  width: 100%;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
}

/* ========== 工单卡片 - 修复版本 ========== */
.order-card {
  background: #fff;
  /* 🔑 关键修复：使用margin而不是父容器padding，确保不会横向溢出 */
  margin: 12rpx 24rpx;
  margin-bottom: 20rpx;
  border-radius: 16rpx;
  padding: 24rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.08);
  /* 🔑 关键修复：position relative + overflow visible让徽章显示 */
  position: relative;
  overflow: visible;
  /* 🔑 确保宽度完整，不会被父容器压缩 */
  width: calc(100% - 48rpx);
  box-sizing: border-box;
  transition: all 0.3s;
}

.order-card:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.12);
}

/* ========== 右上角状态徽章 - 完全修复版 ========== */
.status-badge {
  /* 🔑 绝对定位在卡片右上角 */
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  /* 🔑 足够的内边距让徽章宽敞 */
  padding: 8rpx 16rpx;
  border-radius: 0 16rpx 0 16rpx;
  font-size: 20rpx;
  font-weight: bold;
  color: #fff;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.25);
  /* 🔑 确保徽章在最上层 */
  z-index: 10;
  white-space: nowrap;
  line-height: 1.6;
  pointer-events: auto;
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 70rpx;
  min-height: 40rpx;
  /* 🔑 添加过渡效果 */
  transition: all 0.3s ease;
}

/* ========== 车辆信息头部 ========== */
.vehicle-header {
  display: flex;
  align-items: flex-start;
  gap: 12rpx;
  margin-bottom: 20rpx;
  /* 🔑 为徽章留出空间 */
  padding-right: 80rpx;
  overflow: hidden;
}

.vehicle-icon {
  font-size: 40rpx;
  flex-shrink: 0;
  line-height: 1.2;
}

.vehicle-text {
  font-size: 32rpx;
  font-weight: bold;
  color: #1a1a1a;
  line-height: 1.4;
  word-break: break-word;
  flex: 1;
  /* 🔑 确保文字被截断，不会超出 */
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

/* ========== 车主信息 ========== */
.order-description {
  background: #f8f9fa;
  padding: 16rpx;
  border-radius: 12rpx;
  margin-bottom: 20rpx;
  display: flex;
  gap: 12rpx;
  overflow: hidden;
  align-items: flex-start;
}

.desc-label {
  font-size: 26rpx;
  color: #999;
  font-weight: 500;
  white-space: nowrap;
  flex-shrink: 0;
}

.desc-text {
  font-size: 26rpx;
  color: #666;
  line-height: 1.6;
  flex: 1;
  word-break: break-word;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

/* ========== 工单底部布局 - 两列固定宽度 ========== */
.order-footer {
  /* 🔑 关键修复：使用固定宽度的两列布局 */
  display: flex;
  justify-content: space-between;
  gap: 16rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid #f0f0f0;
  /* 🔑 确保不会超出 */
  width: 100%;
  box-sizing: border-box;
  overflow: hidden;
}

/* 费用项（左侧） */
.footer-item:nth-child(1) {
  flex: 0 1 auto;
  min-width: 0;
}

/* 时间项（右侧） */
.footer-item:nth-child(2) {
  flex: 0 1 auto;
  min-width: 0;
  text-align: right;
}

.footer-item {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.footer-label {
  font-size: 22rpx;
  color: #999;
  white-space: nowrap;
  font-weight: 500;
}

.footer-value {
  font-size: 26rpx;
  color: #333;
  font-weight: 500;
  word-break: break-word;
}

.footer-value.price {
  font-size: 32rpx;
  font-weight: bold;
  color: #ff6b6b;
  white-space: nowrap;
}

.footer-value.time {
  font-size: 24rpx;
  color: #999;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* ========== 空状态 ========== */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 24rpx;
  text-align: center;
  min-height: 60vh;
}

.empty-icon {
  font-size: 120rpx;
  margin-bottom: 24rpx;
  opacity: 0.3;
}

.empty-text {
  font-size: 32rpx;
  color: #999;
  margin-bottom: 12rpx;
  font-weight: 500;
}

.empty-hint {
  font-size: 26rpx;
  color: #bbb;
  line-height: 1.6;
}

/* ========== 创建工单按钮 ========== */
.fab-button {
  position: fixed;
  bottom: 100rpx;
  right: 40rpx;
  width: 120rpx;
  height: 120rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(102, 126, 234, 0.4);
  z-index: 999;
  transition: all 0.3s;
  border: none;
  padding: 0;
}

.fab-button:active {
  transform: scale(0.9);
  box-shadow: 0 4rpx 16rpx rgba(102, 126, 234, 0.4);
}

.fab-icon {
  font-size: 72rpx;
  color: #fff;
  font-weight: 300;
  line-height: 1;
}

/* ========== 加载状态 ========== */
.loading {
  text-align: center;
  padding: 40rpx;
  color: #999;
  font-size: 28rpx;
}

/* ========== 响应式调整 ========== */
@media (max-width: 400px) {
  .vehicle-text {
    font-size: 28rpx;
  }
  
  .page-header {
    padding: 32rpx 24rpx;
  }
  
  .header-title {
    font-size: 36rpx;
  }

  .order-card {
    padding: 20rpx;
  }

  .vehicle-header {
    /* 🔑 在小屏幕上为徽章留出更多空间 */
    padding-right: 90rpx;
  }

  .status-badge {
    font-size: 18rpx;
    padding: 8rpx 14rpx;
    min-width: 65rpx;
    min-height: 36rpx;
  }

  .footer-value.price {
    font-size: 28rpx;
  }

  .footer-value.time {
    font-size: 22rpx;
  }

  .search-bar {
    padding: 16rpx;
  }

  .search-container {
    padding: 16rpx 20rpx;
  }
}

@media (max-width: 375px) {
  .footer-value.price {
    font-size: 26rpx;
  }

  .footer-value.time {
    font-size: 20rpx;
  }

  .status-badge {
    font-size: 16rpx;
    padding: 6rpx 12rpx;
    min-width: 60rpx;
    min-height: 32rpx;
  }

  .vehicle-icon {
    font-size: 36rpx;
  }

  .vehicle-text {
    font-size: 26rpx;
  }

  .vehicle-header {
    padding-right: 85rpx;
  }

  .order-description {
    padding: 12rpx;
  }

  .desc-label {
    font-size: 24rpx;
  }

  .desc-text {
    font-size: 24rpx;
  }

  .order-card {
    padding: 16rpx;
  }
}
